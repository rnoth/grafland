#include <stdio.h>

int main(int argc, char *argv[])
{
        double M = 0;
        double m = 0;
        double extinction = 0;
        double parsecs = 0;
        double extinction_divisor = 1000;
        double real_M = 0;
        double m_minus_M = 0;

        /* (m-M) = distance v*/

        /* log10^d = [(mv - Mv) + 5] / 5 */

        real_M = (M / exitinction_divisor);
        m_minus_M = ( m - real_M);


        return 0;
}
